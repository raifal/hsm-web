# hsm-web

Angular-Anwendung `hsm-web` mit einem Temperatur-Diagramm für 24 Stunden.

Schnellstart:

```bash
npm install
npx ng serve --open
```

Die App verwendet `ng2-charts` + `chart.js`. Der `TemperatureService` liefert Dummy-Werte für 24 Stunden.
# hsm-web